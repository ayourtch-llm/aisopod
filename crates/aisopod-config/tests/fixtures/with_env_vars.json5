// Config with environment variable references
{
  gateway: {
    server: {
      name: "aisopod-gateway-${ENV_SUFFIX:-dev}",
      port: 8080,
    },
    bind: {
      address: "${GATEWAY_HOST:-127.0.0.1}",
    },
  },
  meta: {
    version: "${CONFIG_VERSION:-1.0}",
  },
}
